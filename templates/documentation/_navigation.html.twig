<nav class="navbar navbar-vertical navbar-expand-lg navbar-light">
    <a href="{{ path('landing') }}" class="navbar-brand"><img src="{{ asset('/dist/images/logo.png') }}" alt="Logo"></a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul id="page-nav" class="nav nav-vertical-2 flex-column">
            <li class="nav-item{% if activeSection == "introduction" %} active{% endif %}">
                <a class="nav-link" data-toggle="collapse" href="#getting-started" role="button" aria-expanded="{% if activeSection == "introduction" %}true{% else %}false{% endif %}" aria-controls="menu-1">Getting Started</a>
                <div class="collapse{% if activeSection == "introduction" %} show{% endif %}" id="getting-started" data-parent="#page-nav">
                    <ul class="nav flex-column">
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('documentation') }}">Introduction</a>
                            <a class="nav-link" href="{{ path('docs_getting_started') }}">Getting Started</a>
                        </li>
                    </ul>
                </div>
            </li>
            <li class="nav-item">
                <a class="nav-link{% if activeSection == "calendar" %} active{% endif %}" data-toggle="collapse" href="#calendar" role="button" aria-expanded="{% if activeSection == "calendar" %}true{% else %}false{% endif %}" aria-controls="menu-2">Calendar</a>
                <div class="collapse{% if activeSection == "calendar" %} show{% endif %}" id="calendar" data-parent="#page-nav">
                    <div>
                        <ul class="nav flex-column">
                            <li class="nav-item">
                                <a class="nav-link" href="{{ path('docs_calendar') }}">Index</a>
                            </li>
                            {% for class in calendarClasses %}
                                <li class="nav-item">
                                    <a class="nav-link" href="{{ class.reflectionClass.name | class_url }}">{{ class.shortName }}</a>
                                </li>
                            {% endfor %}
                        </ul>
                    </div>
                </div>
            </li>
            <li class="nav-item">
                <a class="nav-link{% if activeSection == "calendar-holidays" %} active{% endif %}" data-toggle="collapse" href="#calendar-holidays" role="button" aria-expanded="{% if activeSection == "calendar-holidays" %}true{% else %}false{% endif %}" aria-controls="menu-2">Calendar Holidays</a>
                <div class="collapse{% if activeSection == "calendar-holidays" %} show{% endif %}" id="calendar-holidays" data-parent="#page-nav">
                    <div>
                        <ul class="nav flex-column">
                            <li class="nav-item">
                                <a class="nav-link" href="{{ path('docs_calendar_holidays') }}">Index</a>
                            </li>
                            {% for class in calendarHolidaysClasses %}
                                <li class="nav-item">
                                    <a class="nav-link" href="{{ class.reflectionClass.name | class_url }}">{{ class.shortName }}</a>
                                </li>
                            {% endfor %}
                        </ul>
                    </div>
                </div>
            </li>
            <li class="nav-item">
                <a class="nav-link{% if activeSection == "process" %} active{% endif %}" data-toggle="collapse" href="#process" role="button" aria-expanded="{% if activeSection == "process" %}true{% else %}false{% endif %}" aria-controls="menu-2">Process</a>
                <div class="collapse{% if activeSection == "process" %} show{% endif %}" id="process" data-parent="#page-nav">
                    <div>
                        <ul class="nav flex-column">
                            <li class="nav-item">
                                <a class="nav-link" href="{{ path('docs_process') }}">Index</a>
                            </li>
                            {% for class in processClasses %}
                                <li class="nav-item">
                                    <a class="nav-link" href="{{ class.reflectionClass.name | class_url }}">{{ class.shortName }}</a>
                                </li>
                            {% endfor %}
                        </ul>
                    </div>
                </div>
            </li>
            <li class="nav-item">
                <a class="nav-link{% if activeSection == "retry" %} active{% endif %}" data-toggle="collapse" href="#retry" role="button" aria-expanded="{% if activeSection == "retry" %}true{% else %}false{% endif %}" aria-controls="menu-2">Retry</a>
                <div class="collapse{% if activeSection == "retry" %} show{% endif %}" id="retry" data-parent="#page-nav">
                    <div>
                        <ul class="nav flex-column">
                            <li class="nav-item">
                                <a class="nav-link" href="{{ path('docs_retry') }}">Index</a>
                            </li>
                            {% for class in retryClasses %}
                                <li class="nav-item">
                                    <a class="nav-link" href="{{ class.reflectionClass.name | class_url }}">{{ class.shortName }}</a>
                                </li>
                            {% endfor %}
                        </ul>
                    </div>
                </div>
            </li>
        </ul>
    </div>
</nav>