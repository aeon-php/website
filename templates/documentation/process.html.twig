{% extends 'docs.html.twig' %}

{% block title %}Aeon PHP - Process - Documentation{% endblock %}

{% block body %}
    <div class="row doc-content-body">
        <article class="col-md-9">
        <section id="calendar-interface">
            <h5 class="section-title-2">Process</h5>
            <hr/>
            <ul>
                {% for version, src in versions %}
                    <li><a href="{{ path('docs_process_version', { version: version }) }}">{{ version }}</a></li>
                {% endfor %}
            </ul>
        </section>
        </article>
    </div>
{% endblock %}
